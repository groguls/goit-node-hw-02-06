const {
  getAll,
  getByID,
  add,
  removeById,
  updateById,
  updateStatusContact,
} = require("./controllers");

module.exports = {
  getAll,
  getByID,
  add,
  removeById,
  updateById,
  updateStatusContact,
};
